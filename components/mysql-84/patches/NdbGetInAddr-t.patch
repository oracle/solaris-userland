Makes tests consistent on Solaris build hosts.

Solaris specific. Not suitable for upstream.

--- mysql-8.4.10/storage/ndb/src/common/portlib/CMakeLists.txt
+++ mysql-8.4.10/storage/ndb/src/common/portlib/CMakeLists.txt
@@ -84,6 +84,9 @@ NDB_ADD_TEST(NdbHW-t NdbHW.cpp LIBS ndbm
 
 NDB_ADD_TEST(NdbDir-t NdbDir.cpp LIBS ndbmgmapi ndbgeneral ndbportlib)
 
+# This test depends on host network configuration and is unstable on Solaris.
+if (NOT CMAKE_SYSTEM_NAME STREQUAL "SunOS")
 NDB_ADD_TEST(NdbGetInAddr-t NdbTCP.cpp LIBS ndbgeneral ${LIBSOCKET} ${LIBNSL})
+endif()
 NDB_ADD_TEST(NdbProcess-t NdbProcess.cpp LIBS ndbmgmapi ndbgeneral ndbportlib)
 NDB_ADD_TEST(NdbTimestamp-t NdbTimestamp.cpp LIBS ndbgeneral ndbportlib)
