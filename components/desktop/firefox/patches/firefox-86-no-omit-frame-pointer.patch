Firefox should not be compiled with -fomit-frame-pointer to allow better core file debugging.

--- firefox-153.0/build/moz.configure/toolchain.configure
+++ firefox-153.0/build/moz.configure/toolchain.configure
@@ -2909,6 +2909,7 @@
         or msan
         or asan
         or ubsan
+        or (target.os == 'SunOS')
         or (target.os == "WINNT" and target.cpu in ("x86", "aarch64"))
         or target.os == "OSX"
     )
