With upstream GCC 15 patch it still needs getenv() declaration.

--- nettle-3.7.3/getopt.c
+++ nettle-3.7.3/getopt.c
@@ -30,6 +30,7 @@ # include <config.h>
 #endif
 
 #include <stdio.h>
+#include <stdlib.h>
 
 /* Comment out all this code if we are using the GNU C Library, and are not
    actually compiling the library itself.  This code is part of the GNU C
