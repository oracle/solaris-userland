Solaris specific patch

not suitable for upstream


--- xorg-server-21.1.20/dix/main.c
+++ xorg-server-21.1.20/dix/main.c
@@ -78,6 +78,8 @@ #include <dix-config.h>
 #include <version-config.h>
 #endif
 
+#include <sys/stat.h>
+#include <stdio.h>
 #include <X11/X.h>
 #include <X11/Xos.h>            /* for unistd.h  */
 #include <X11/Xproto.h>
@@ -124,6 +126,34 @@ #endif /* SUNSOFT */
 
 extern void Dispatch(void);
 
+#if defined(sun)
+extern const char * GetAuthFilename(void);
+static char sym_authfile[40];
+
+#define LOCK_PREFIX    "/var/xauth/"
+
+/*
+ * Setup /var/xauth/$DISPLAY, which is a symlink to the
+ * actually auth file.
+*/
+static void
+SetupXauthFile(const char *filename)
+{
+        snprintf(sym_authfile, 40, LOCK_PREFIX "%s", display);
+
+        mkdir(LOCK_PREFIX, S_IRWXU);
+
+        remove(sym_authfile);
+        symlink(filename, sym_authfile);
+}
+
+void
+RemoveXauthSymFile(void)
+{
+        remove(sym_authfile);
+}
+#endif
+
 CallbackListPtr RootWindowFinalizeCallback = NULL;
 
 int
@@ -132,6 +162,10 @@ {
     int i;
     HWEventQueueType alwaysCheckForInput[2];
 
+#if defined(sun)
+    const char *xauthfile = NULL;
+#endif
+
     display = "0";
 
     InitRegions();
@@ -142,6 +176,12 @@ CheckUserAuthorization();
 
     ProcessCommandLine(argc, argv);
 
+#if defined(sun)
+    xauthfile = GetAuthFilename();
+    if (xauthfile)
+        SetupXauthFile(xauthfile);
+#endif
+
     alwaysCheckForInput[0] = 0;
     alwaysCheckForInput[1] = 1;
     while (1) {
--- xorg-server-21.1.20/hw/xfree86/os-support/solaris/sun_init.c
+++ xorg-server-21.1.20/hw/xfree86/os-support/solaris/sun_init.c
@@ -50,6 +50,9 @@ */
 #define	CONSOLE_VTNO	1
 #define	SOL_CONSOLE_DEV	"/dev/console"
 
+/* For use of VT_SETDISPLOGIN in dtlogin.c */
+extern int xf86ConsoleFd;
+
 static Bool KeepTty = FALSE;
 static Bool UseConsole = FALSE;
 
@@ -221,6 +224,8 @@ FatalError("xf86OpenConsole: VT_SETMODE
 
             if (ioctl(xf86Info.consoleFd, VT_SETDISPINFO, atoi(display)) < 0)
                 xf86Msg(X_WARNING, "xf86OpenConsole: VT_SETDISPINFO failed\n");
+
+            xf86ConsoleFd = xf86Info.consoleFd;
         }
 #endif
 
--- xorg-server-21.1.20/os/log.c
+++ xorg-server-21.1.20/os/log.c
@@ -115,6 +115,10 @@ #ifndef DEFAULT_LOG_FILE_VERBOSITY
 #define DEFAULT_LOG_FILE_VERBOSITY	3
 #endif
 
+#ifdef sun
+extern void RemoveXauthSymFile(void);
+#endif
+
 static FILE *logFile = NULL;
 static int logFileFd = -1;
 static Bool logFlush = FALSE;
@@ -1009,6 +1013,9 @@ #endif
     VErrorFSigSafe(f, args);
     va_end(args);
     ErrorFSigSafe("\n");
+#ifdef sun
+    RemoveXauthSymFile();
+#endif
     if (!beenhere)
         OsVendorFatalError(f, args2);
     va_end(args2);
