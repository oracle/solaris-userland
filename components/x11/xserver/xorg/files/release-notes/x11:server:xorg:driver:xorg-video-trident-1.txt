The trident driver for Xorg for Trident graphics is no longer provided.

If you have an xorg.conf file which specifies this device driver, you will
need to change it to use the "vesa" driver instead.
