!!! GPGME is a library so its primary use is within software.
Since GPGME interacts with GnuPG, ensure basic GPG operations work:

1. Generate a test key. Run the command and follow the prompts:
$ gpg --gen-key

2. List the created key:
$ gpg --list-keys

