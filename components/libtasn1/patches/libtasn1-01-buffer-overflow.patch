Source:
Internal

Info:
To take out buffer overflow issue.

Status:
Upstream seems not to bother about case 'str == NULL' like we do.
This is the only difference

--- libtasn1-4.21.0/lib/coding.c
+++ libtasn1-4.21.0/lib/coding.c
@@ -358,7 +358,10 @@ _asn1_object_id_der (const char *str, un
   int len_len, counter, max_len;
   char *temp, *n_end, *n_start;
   uint64_t val, val1 = 0;
-  int str_len = _asn1_strlen (str);
+  size_t str_len = (str == NULL) ? 0 : _asn1_strlen (str);
+
+  if ((str_len + 2) < 2)
+    return ASN1_MEM_ALLOC_ERROR;
 
   max_len = *der_len;
   *der_len = 0;
