Give the tests longer timeout to make them pass.

Not suitable for upstream.

--- libsoup-3.6.5/tests/meson.build
+++ libsoup-3.6.5/tests/meson.build
@@ -227,7 +227,7 @@ args : ['--debug'],
     env : env,
     is_parallel : test.get('parallel', true),
     depends : test.get('depends', []),
-    timeout : 60,
+    timeout : 300,
     protocol : 'tap',
   )
 endforeach
