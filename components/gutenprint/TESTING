Top level tests are disabled in Makefile because run-weavetest.test runs for hours. It is necessary to run all the tests after upgrade. Run

$ gmake test

to check for new issues on both platforms. Be patient.
