On a machine with installed the updated libksba do

ksba-config --version

mkdir /tmp/test-libksba
cd /tmp/test-libksba
GNUPGHOME="$PWD" /usr/bin/gpgsm --import /etc/certs/CA/Oracle_Corporation_Solaris_Object_Signing_CA.pem
GNUPGHOME="$PWD" /usr/bin/gpgsm --list-keys
