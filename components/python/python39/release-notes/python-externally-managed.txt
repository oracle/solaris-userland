Python environment is now marked as "externally managed", meaning that pip
no longer allows installation directly onto the system (including the user
install directory).

To install non-Solaris-packaged Python packages and applications,
use virtual environments or newly introduced pipx.

See #BLOG POST for more information.
