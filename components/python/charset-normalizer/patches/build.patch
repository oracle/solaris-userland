Charset normalizer can be build without optional Mypy compiler.

--- charset_normalizer-3.4.2/pyproject.toml
+++ charset_normalizer-3.4.2/pyproject.toml
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["setuptools", "setuptools-scm", "mypy>=1.4.1,<=1.15.0"]
+requires = ["setuptools", "setuptools-scm"]
 build-backend = "setuptools.build_meta"
 
 [project]
