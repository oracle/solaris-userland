Csh activation script is incorrectly using : modifiers, which are not
available for environment variables.

Can be offered upstream.

--- virtualenv-20.36.1/src/virtualenv/activation/cshell/activate.csh
+++ virtualenv-20.36.1/src/virtualenv/activation/cshell/activate.csh
@@ -12,8 +12,8 @@ deactivate nondestructive
 
 setenv VIRTUAL_ENV __VIRTUAL_ENV__
 
-set _OLD_VIRTUAL_PATH="$PATH:q"
-setenv PATH "$VIRTUAL_ENV:q/"__BIN_NAME__":$PATH:q"
+set _OLD_VIRTUAL_PATH="$PATH"
+setenv PATH "$VIRTUAL_ENV/"__BIN_NAME__":$PATH"
 
 if (__TCL_LIBRARY__ != "") then
     if ($?TCL_LIBRARY) then
@@ -32,7 +32,8 @@ endif
 if (__VIRTUAL_PROMPT__ != "") then
     setenv VIRTUAL_ENV_PROMPT __VIRTUAL_PROMPT__
 else
-    setenv VIRTUAL_ENV_PROMPT "$VIRTUAL_ENV:t:q"
+    set env_name = `basename "$VIRTUAL_ENV"`
+    setenv VIRTUAL_ENV_PROMPT "$env_name"
 endif
 
 if ( $?VIRTUAL_ENV_DISABLE_PROMPT ) then
@@ -54,7 +55,7 @@ if ( $do_prompt == "1" ) then
         if ( "$prompt:q" =~ *"$newline:q"* ) then
             :
         else
-            set prompt = '('"$VIRTUAL_ENV_PROMPT:q"') '"$prompt:q"
+            set prompt = '('"$VIRTUAL_ENV_PROMPT"') '"$prompt:q"
         endif
     endif
 endif
--- virtualenv-20.36.1/tests/unit/activation/test_csh.py
+++ virtualenv-20.36.1/tests/unit/activation/test_csh.py
@@ -53,7 +53,7 @@ def test_cshell_tkinter_generation(tmp_p
         assert "setenv TCL_LIBRARY ''" in content
 
 
-def test_csh(activation_tester_class, activation_tester):
+def test_tcsh(activation_tester_class, activation_tester):
     exe = f"tcsh{'.exe' if sys.platform == 'win32' else ''}"
     if which(exe):
         version_text = check_output([exe, "--version"], text=True, encoding="utf-8")
@@ -61,6 +61,17 @@ def test_csh(activation_tester_class, ac
         if version >= Version("6.24.14"):
             pytest.skip("https://github.com/tcsh-org/tcsh/issues/117")
 
+    class Tcsh(activation_tester_class):
+        def __init__(self, session):
+            super().__init__(CShellActivator, session, "tcsh", "activate.csh", "csh")
+
+        def print_prompt(self):
+            return "echo 'source \"$VIRTUAL_ENV/bin/activate.csh\"; echo $prompt' | tcsh -i"
+
+    activation_tester(Tcsh)
+
+
+def test_csh(activation_tester_class, activation_tester):
     class Csh(activation_tester_class):
         def __init__(self, session) -> None:
             super().__init__(CShellActivator, session, "csh", "activate.csh", "csh")
