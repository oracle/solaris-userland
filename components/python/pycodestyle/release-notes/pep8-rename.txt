The /usr/bin/pep8 tool is now delivered as /usr/bin/pycodestyle due to
it being renamed by the "Python Code Quality Authority" project.

All command line script invocations and Python package imports must use the new name.
