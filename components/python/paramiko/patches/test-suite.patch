We don't have pytest_relaxed and invoke packages is Solaris. Because of that,
raises decorator is implemented here.

Neither icecream nor invoke is available on Solaris, but thankfully, they
are not strictly required.

This is not intended for upstream.

--- paramiko-5.0.0/tests/test_client.py
+++ paramiko-5.0.0/tests/test_client.py
@@ -33,7 +33,15 @@ from tempfile import mkstemp
 from unittest.mock import patch
 
 import pytest
-from pytest_relaxed import raises
+def raises(expected):
+    def outer(func):
+        def inner(*args, **kwargs):
+            try:
+                func(*args, **kwargs)
+            except expected:
+                pass
+        return inner
+    return outer
 
 import paramiko
 from paramiko import SSHClient
--- paramiko-5.0.0/tests/conftest.py
+++ paramiko-5.0.0/tests/conftest.py
@@ -5,9 +5,6 @@ import threading
 from pathlib import Path
 
 import pytest
-from icecream import ic
-from icecream import install as install_ic
-from invoke.vendor.lexicon import Lexicon
 
 from paramiko import (
     SFTP,
@@ -22,10 +19,6 @@ from ._loop import LoopSocket
 from ._stub_sftp import StubServer, StubSFTPServer
 from ._util import _support
 
-# Better print() for debugging - use ic()!
-install_ic()
-ic.configureOutput(includeContext=True)
-
 
 # Perform logging by default; pytest will capture and thus hide it normally,
 # presenting it on error/failure. (But also allow turning it off when doing
--- paramiko-5.0.0/pyproject.toml
+++ paramiko-5.0.0/pyproject.toml
@@ -11,7 +11,6 @@ authors = [{ name = "Jeff Forcier", emai
 dependencies = [
   "bcrypt>=3.2",
   "cryptography>=3.3",
-  "invoke>=2.0",
 ]
 
 classifiers = [
