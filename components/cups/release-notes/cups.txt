Starting with Oracle Solaris 11.4.75 the CUPS SMF services, cups/scheduler and
cups/in-lpd, are disabled by default. If you intend to keep using the services,
you must enable them via the "svcadm enable <service>" command.
