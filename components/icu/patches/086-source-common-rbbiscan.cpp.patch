Fix a memory leak
http://bugs.icu-project.org/trac/ticket/12374
--- icu4c-77_1/icu/source/common/rbbiscan.cpp
+++ icu4c-77_1/icu/source/common/rbbiscan.cpp
@@ -804,7 +804,13 @@ //
     // Add the new set to the set hash table.
     //
     el = static_cast<RBBISetTableEl*>(uprv_malloc(sizeof(RBBISetTableEl)));
-    UnicodeString *tkey = new UnicodeString(s);
+    UnicodeString *tkey;
+    try
+    {
+        tkey = new UnicodeString(s);
+    } catch (...) {
+        tkey = nullptr;
+    }
     if (tkey == nullptr || el == nullptr || setToAdopt == nullptr) {
         // Delete to avoid memory leak
         delete tkey;
