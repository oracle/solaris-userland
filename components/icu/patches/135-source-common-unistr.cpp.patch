getBuffer handling is tracked upstream as
http://www.icu-project.org/trac/ticket/12378
--- icu4c-77_1/icu/source/common/unistr.cpp
+++ icu4c-77_1/icu/source/common/unistr.cpp
@@ -666,6 +666,9 @@ if (result.isBogus()) {
         return result;
     }
     const char16_t *array = getBuffer();
+    if ( array == NULL ) {
+        return result;
+    }
     int32_t len = length();
     int32_t prev = 0;
     for (int32_t i=0;;) {
