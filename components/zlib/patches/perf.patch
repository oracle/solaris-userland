Patch origin: in-house
Patch status: deflate.c part: Solaris-specific; not suitable for upstream

--- zlib-1.3.2/deflate.c
+++ zlib-1.3.2/deflate.c
@@ -60,6 +60,7 @@ include such an acknowledgment, I would
   copyright string in the executable of your product.
  */
 
+#ifndef LONGEST_MATCH_ONLY
 typedef enum {
     need_more,      /* block not completed, need more input or more output */
     block_done,     /* block flush performed */
@@ -77,6 +78,7 @@ local block_state deflate_slow(deflate_s
 #endif
 local block_state deflate_rle(deflate_state *s, int flush);
 local block_state deflate_huff(deflate_state *s, int flush);
+#endif /* ! LONGEST_MATCH_ONLY */
 
 /* ===========================================================================
  * Local data
@@ -85,6 +87,7 @@ */
 #define NIL 0
 /* Tail of hash chains */
 
+#ifndef LONGEST_MATCH_ONLY
 #ifndef TOO_FAR
 #  define TOO_FAR 4096
 #endif
@@ -1375,7 +1378,9 @@ ds->bl_desc.dyn_tree = ds->bl_tree;
     return Z_OK;
 #endif /* MAXSEG_64K */
 }
+#endif /* ! LONGEST_MATCH_ONLY */
 
+#if defined(ORIG_LONGEST_MATCH) || defined(ORIG_LONGEST_MATCH_GLOBAL)
 #ifndef FASTEST
 /* ===========================================================================
  * Set match_start to the longest match starting at the given string and
@@ -1386,7 +1391,12 @@ * IN assertions: cur_match is the head o
  *   string (strstart) and its distance is <= MAX_DIST, and prev_length >= 1
  * OUT assertion: the match length is not greater than s->lookahead.
  */
-local uInt longest_match(deflate_state *s, IPos cur_match) {
+#ifdef ORIG_LONGEST_MATCH_GLOBAL
+uInt longest_match(deflate_state *s, IPos cur_match)
+#else
+local uInt longest_match(deflate_state *s, IPos cur_match)
+#endif
+{
     unsigned chain_length = s->max_chain_length;/* max hash chain length */
     Bytef *scan = s->window + s->strstart;      /* current string */
     Bytef *match;                               /* matched string */
@@ -1528,6 +1538,7 @@ && --chain_length != 0);
     if ((uInt)best_len <= s->lookahead) return (uInt)best_len;
     return s->lookahead;
 }
+#endif /* ORIG_LONGEST_MATCHT */
 
 #else /* FASTEST */
 
@@ -1587,6 +1598,7 @@ }
 
 #endif /* FASTEST */
 
+#ifndef LONGEST_MATCH_ONLY
 #ifdef ZLIB_DEBUG
 
 #define EQUAL 0
@@ -2183,3 +2195,5 @@ if (s->sym_next)
         FLUSH_BLOCK(s, 0);
     return block_done;
 }
+
+#endif /* ! LONGEST_MATCH_ONLY */
